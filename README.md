# thermal_printer_susana

```shell
pip install python-escpos firebase-admin google-cloud-storage google-cloud-firestore
sudo chmod a+rwx /dev/usb/lp1 # or lp0
git clone git@github.com:jorisgu/thermal_printer_susana.git
mkdir images
cd thermal_printer_susana
export GOOGLE_APPLICATION_CREDENTIALS="key.json"
python main.py
```
